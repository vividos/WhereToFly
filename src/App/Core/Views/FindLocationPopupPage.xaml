<?xml version="1.0" encoding="UTF-8"?>
<views:BasePopupPage xmlns="http://xamarin.com/schemas/2014/forms"
                     xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                     xmlns:views="clr-namespace:WhereToFly.App.Core.Views"
                     x:Class="WhereToFly.App.Core.Views.FindLocationPopupPage">

    <!-- popup content -->
    <StackLayout
        VerticalOptions="Center"
        HorizontalOptions="Center"
        HeightRequest="140"
        WidthRequest="300"
        BackgroundColor="{DynamicResource PageBackgroundColor}"
        Padding="20">

        <!-- text, entry control to enter location text and button -->
        <Label Text="Find location" FontSize="Large" />
        <Entry Placeholder="enter location" x:Name="locationEntry" />
        <Button Text="Find" Clicked="OnClickedFindButton" />
    </StackLayout>

</views:BasePopupPage>
