<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WhereToFly.App.Core.Controls.ContextMenu">
    <Grid>
        <!-- Context menu 3-dot button -->
        <ffimageloadingsvg:SvgCachedImage Source="{Binding ContextMenuImageSource, Mode=OneWay}"
                                          ReplaceStringMap="{DynamicResource SvgImageFillDark}"
                                          WidthRequest="32"
                                          HeightRequest="32"
                                          HorizontalOptions="Center"
                                          VerticalOptions="Center">
            <ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
                <TapGestureRecognizer NumberOfTapsRequired="1"
                                      Command="{Binding ContextMenuCommand}" />
            </ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
        </ffimageloadingsvg:SvgCachedImage>
    </Grid>
</ContentView>
