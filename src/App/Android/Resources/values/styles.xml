<?xml version="1.0" encoding="utf-8" ?>
<resources>

  <!-- Main theme for the app; add modifications here -->
  <style name="MainTheme" parent="MainTheme.Base">
  </style>

  <!-- Base theme applied no matter what API -->
  <style name="MainTheme.Base" parent="Theme.AppCompat.DayNight.DarkActionBar">
    <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
    <item name="windowNoTitle">true</item>
    <!--We will be using the toolbar so no need to show ActionBar-->
    <item name="windowActionBar">false</item>
    <!-- Set theme colors from https://aka.ms/material-colors -->
    <!-- colorPrimary is used for the default action bar background -->
    <item name="colorPrimary">@color/primary</item>
    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">@color/primary_dark</item>
    <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
    <item name="colorAccent">@color/accent</item>
    <!-- color of activated highlighted items, e.g. selected list view entries -->
    <item name="@android:colorActivatedHighlight">@color/primary</item>
    <item name="@android:colorControlHighlight">@color/primary</item>
    <item name="@android:colorPressedHighlight">@color/primary</item>
    <item name="@android:colorLongPressedHighlight">@color/accent</item>
    <item name="@android:colorFocusedHighlight">@color/primary</item>
    <item name="@android:activatedBackgroundIndicator">@color/primary</item>
    <!-- You can also set colorControlNormal, colorControlActivated and colorSwitchThumbNormal. -->
    <item name="windowActionModeOverlay">true</item>
    <!-- Style of date picker dialog -->
    <item name="android:datePickerDialogTheme">@style/AppCompatDialogStyle</item>
  </style>

  <!-- Style for date picker -->
  <style name="AppCompatDialogStyle" parent="Theme.AppCompat.DayNight.Dialog">
    <item name="colorAccent">@color/accent</item>
  </style>

  <!-- Splash screen style -->
  <style name="SplashTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/splash_screen</item>
  </style>

  <style name="SplashTheme" parent="SplashTheme.Base">
    <item name="colorPrimary">@color/primary</item>
    <item name="colorPrimaryDark">@color/primary_dark</item>
    <item name="colorAccent">@color/accent</item>
  </style>
</resources>
